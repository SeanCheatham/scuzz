[package]
name = "counter"
version = "0.1.0"

[targets.native]
kind = "executable"
main = "Main"

[dependencies]
shared = { path = "../shared" }

[ui]
default_runtime = "headless"
headless_size = [200, 120]
headless_scale = 1.0
