record Point(x: Int where x >= 0, y: Int where y + 1 > y && y == y)

def sum(p: Point): Int =
  p.x + p.y

@main def main: IO[Unit] =
  IO.println(Str.concat("record:", Str.fromInt(sum(Point(3, 5)))))
