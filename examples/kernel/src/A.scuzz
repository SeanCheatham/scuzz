enum Msg:
  case Hi
  case Bye

type Tag = String
def one(): Int =
  1

private def hidden(): String =
  "a"

def tag(): String =
  labeled(hidden())

def labeled(t: Tag): Tag =
  t

def greet(): String =
  Msg.Hi match {
    case Msg.Hi => "hello-a"
    case Msg.Bye => "bye-a"
  }

