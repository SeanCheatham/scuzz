[package]
name = "editor"
version = "0.1.0"
description = "Package Index Book: Live stays the job, package title, no tab row"

[ui]
default_runtime = "desktop"
headless_size = [960, 560]
headless_scale = 1.0
tap_button = 0
