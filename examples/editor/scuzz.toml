[package]
name = "editor"
version = "0.1.0"
description = "Package Index Book: Session chips from campaign dumps, Live stays the job"

[ui]
default_runtime = "desktop"
headless_size = [960, 560]
headless_scale = 1.0
tap_button = 0
