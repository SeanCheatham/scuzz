[package]
name = "editor"
version = "0.1.0"
description = "Open a path from Sys.args, edit in View.editor, check JSON, save"

[ui]
default_runtime = "headless"
headless_size = [720, 400]
headless_scale = 1.0
tap_button = 3
