def afterPlusShowsOne(t: Timeline): Verdict =
  Verdict.afterHit(t, "button:+1", "text:count = 1")

def onPlusMoves(t: Timeline): Verdict =
  Verdict.onHit(t, "button:+1", __tup => __tup match {
  case (_before, _after) => true
})

