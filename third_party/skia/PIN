# Pinned sk_capi Skia CPU prebuilt (as-needed skia-cpu-vN Release asset).
# package_release.sh fetches this into the release tree when URL is set.
# Leave URL empty until the first skia-cpu workflow publishes an asset.
#
# Format: key=value lines
skia_branch=chrome/m131
triple=x86_64-unknown-linux-gnu
# Example after first publish:
# url=https://github.com/OWNER/REPO/releases/download/skia-cpu-v0.1/skia-x86_64-unknown-linux-gnu-cpu.tar.gz
url=
