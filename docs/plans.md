# Short-term plan

## Next

**Poll Sys.readLine.** HUMANS Headless + stamp-watch + live dump + live inject + Net listen/conn/`httpGet` poll are in. DNS `getaddrinfo` and stdin `readLine` still block the fiber. Still not Flutter VM patching.
