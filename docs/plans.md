# Short-term plan

## Next

Memory v0 for lists is in (unshared spine free + `View.each` pump-flat). Skip a tracing collector. Pick the next `gaps.md` residual that is a Headless vertical slice — not OS threads, LSP, or Windows.
