# Next slice

Self-hosting staged rewrite 4 remainder: **compile `examples/`, compile itself, and Rust retirement** ([`vision.md`](vision.md#self-hosting)).

CLI core, driver core, and test/fuzz core are in (`examples/cli`, `examples/compiler`, `cli-ok`). The product CLI is still the Rust binary.

- Finish the slice: compile `examples/`, compile itself.
- The Rust binary retires when that campaign is green.

Current locks: [`vision.md`](vision.md). Ranked gaps: [`gaps.md`](gaps.md).
