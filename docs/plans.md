# Short-term plan

## Next

**Stage 1/2 run --watch keep-alive.** Stage 0 `[ui]` `run --watch` keeps the process and stamp-reloads the View tree (not new machine code). Next: the same keep-alive on the shipped CLI. Still not Flutter VM patching. Do not document `watch` as hot reload.
